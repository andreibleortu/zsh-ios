/target
Cargo.lock

# Editor / OS junk
.DS_Store
**/.DS_Store

# One-off agent-definition files — per-checkout, never commit
.claude/agents/

# cargo-fuzz build artifacts
fuzz/target/

# cargo-fuzz runtime output — corpus (auto-generated) and crash artifacts.
# The hand-written seed files (seeds.txt) are tracked separately via git add -f.
fuzz/corpus/
fuzz/artifacts/
